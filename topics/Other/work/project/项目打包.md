 # 项目打包

 ## 前端
    npm run build-webjar
    如果构建过程中出现内存不足（JavaScript heap out of memory），可以用内存更大的命令：
    node --max_old_space_size=8192 ./node_modules/@angular/cli/bin/ng build --prod --baseHref=./
    注意：当前终端若正在跑 npm start，建议另开一个终端窗口再执行打包，避免互相影响, 还要注意node版本