# 用户模块知识笔记

## mybatis plus 
    1. mybatis plus 的分页插件在 v3.5.9的版本之后就必须独立安装分页插件的依赖


## spring mvc
    1. 由于前端js的精度范围限制, 如果我们后端返回的id是long类型且范围过大, 会导致前端精度丢失, 可以在后端加全局Json配置
    将整个spring mvc接口返回值的长整型数字转换为字符串返回, 从而集中解决问题